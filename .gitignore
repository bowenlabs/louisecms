node_modules/
dist/
.astro/
.wrangler/
.dev.vars
# astro:env reads local dev config/secrets from .env (see workers/sandbox/.env.example)
.env
.env.*
!.env.example
*.log
.DS_Store
coverage/
.turbo/
.vite/
*.tsbuildinfo
# npm/vp pack tarballs (build artifacts, never committed)
*.tgz
# Static docs bundle folded into the marketing Worker's assets at build time
# (scripts/ci-build.sh copies workers/docs/dist here).
workers/site/public/_docs/
# (The CI scaffold smoke test now scaffolds into $RUNNER_TEMP, outside the repo,
# so there's nothing in-tree left to ignore.)
